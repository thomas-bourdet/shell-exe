#!/bin/bash


# for = Début de la boucle + initiateur de la variable
# i = variable
# in = indicateur pour for que la variable va de la première valeur et la dernière valeur compris dans les bornes.
# {} = Bornes qui permette de délimiter la valeur max et minimum de la variable


for i in {1..10}
do
echo "Je suis un script qui arrive à faire une boucle $i"
done
